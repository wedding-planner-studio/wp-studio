# Allow all crawlers
User-agent: *

# Disallow paths that shouldn't be indexed
Disallow: /api/
Disallow: /admin/
Disallow: /sign-in/
Disallow: /logout/

# Allow specific OG image API if needed (adjust if necessary)
# Allow: /api/og/

# Point to the sitemap
Sitemap: https://evana.mx/sitemap.xml